{
  "name": "documenter",
  "version": "1.0.0",
  "scripts": {
    "start": "tsx src/cli.ts",
    "dev": "tsx src/cli.ts",
    "build": "mkdir -p bin && npx esbuild src/cli.ts --bundle --platform=node --target=node22 --outfile=bin/documenter.js && echo '#!/usr/bin/env node' | cat - bin/documenter.js > bin/documenter && chmod +x bin/documenter && rm bin/documenter.js"
  },
  "dependencies": {
    "@openai/agents": "^0.0.2",
    "chalk": "^5.4.1",
    "dotenv": "^16.5.0",
    "openai": "^5.1.1",
    "zod": "^3.25.51"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "tsx": "^4.19.4",
    "esbuild": "^0.20.0"
  }
}
